# SKILL.md（已迁移）

本项目的 skill 已注册为**项目级 skill**，真源位于：

`.claude/skills/novel-to-screenplay-mvp/SKILL.md`

放在该目录后，Claude Code 会将其作为可调用的项目级 skill 加载（仅对本仓库生效，不影响全局）。

代码注释与文档中以 "SKILL.md section N" 形式引用的章节号，均指向上述真源文件。
